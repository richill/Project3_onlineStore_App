<%= form_for(@productmain) do |f| %>
  <% if @productmain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@productmain.errors.count, "error") %> prohibited this productmain from being saved:</h2>

      <ul>
      <% @productmain.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div><br>
  <div class="actions">
    <%= f.submit %>
  </div><br>
<% end %>
