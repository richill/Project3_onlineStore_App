<%= form_for(@product_brand_name) do |f| %>
  <% if @product_brand_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_brand_name.errors.count, "error") %> prohibited this product_brand_name from being saved:</h2>

      <ul>
      <% @product_brand_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %><br>

  <div class="field">
    <%= f.text_field :name %>
  </div><br>
  <div class="actions">
    <%= f.submit %>
  </div><br>
<% end %>
